# ---- data & model weights: do NOT commit (large/binary; >100MB GitHub limit) ----
# datasets: push CRDLD + CRBD source (user requested); exclude only the
# generated CALM-Row detection set (reproducible via prepare_crdld.py)
datasets/CRDLD_yolo/
datasets/CRDLD/
datasets/CRBD/
datasets/CornRobot*/
datasets/Cabbage*/
weights/
runs/
*.pt
*.pth
*.onnx
*.engine
*.rknn
*.torchscript
_cleanup_backup_sgc.zip

# ---- generated A/B outputs ----
results/
results_*.csv
calib_*.csv
gtlines_*.json
datasets/CRDLD_yolo.local.yaml

# ---- auto-generated paper assets ----
# Tables + result figures ARE tracked: training runs on Linux (no LaTeX) and the
# paper is compiled on Windows, so these must travel via git. Only the raw
# per-frame overlays are ignored (the montage fig_qualitative.pdf is kept).
paper/figures/qualitative/

# ---- python ----
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/

# ---- LaTeX build artifacts (keep .tex, .bib, figures/*.pdf, main.pdf) ----
paper/main.aux
paper/main.bcf
paper/main.bbl
paper/*.bbl-SAVE-ERROR
paper/main.blg
paper/main.log
paper/main.out
paper/main.run.xml
paper/main.toc
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz
paper/sections/*.aux

# ---- large EndNote reference-PDF library (citations live in refs.bib) ----
paper/ref/

# ---- large local docs / field media (NOT part of the codebase) ----
*.MOV
*.mov
*.mp4
*.avi
_vidframes/
/main.pdf
/*.docx

# ---- OS ----
.DS_Store
Thumbs.db
