%!TEX root = ../main.tex
% =====================================================================
% MATERIALS AND METHODS
% "Detector-free, training-free crop-row guidance for a cabbage field
%  robot: an in-field validation study"
%
% Honest positioning: APPLIED in-field VALIDATION paper. The CV pipeline
% is well-engineered STOCK parts (Excess-Green column-peak tracker ->
% robust Huber IRLS quadratic fit x=P(y) -> look-ahead tangent heading +
% cross-track -> median/EMA/physical jump-gate temporal fusion). NO
% neural detector, NO training, NO method-novelty claim. The temporal
% back-end is positioned as the dominant robustness lever, but the
% robustness claim is error-vs-GT + gate selectivity (NOT a jump count).
%
% Measured: 99.9% row-emission (1481/1482), tertile emission, cross-track
% magnitude (cm), heading and cross-track error vs a 150-frame human-labelled
% subset, the temporal ablation, and the fit-arm / front-end / trained-detector
% (YOLOv5) baselines. Single annotator; inter-annotator agreement = future work.
% =====================================================================

\section{Materials and methods}
\label{sec:method}

\noindent This is a validation and deployment contribution, not a new method: the pipeline is assembled
entirely from established, stock \texttt{cv2}/\texttt{numpy} components with no neural detector and no
training, so every frame is a test frame and no data can leak into the evaluation. This section describes the
platform, the pipeline, and the evaluation protocol. The accuracy results are measured against a
human-labelled subset of $150$ frames -- row emission, cross-track magnitude, heading and cross-track error,
the temporal ablation, and the fit-arm and trained-detector baselines -- by a single annotator, so an
inter-annotator agreement floor on the smallest meaningful error is not established and is left to future
work.



% =====================================================================
\subsection{Robot platform and field data}
\label{sec:method:platform}
% =====================================================================

\noindent The data were collected from a single ground robot (Figure~\ref{fig:robot}), a four-wheel straddle
platform that drives along a crop row with a forward-view camera mounted to look down the inter-row corridor.
The field runs were recorded as the robot drove along the cabbage rows at a nominal forward speed of
approximately $0.14$\,m\,s$^{-1}$. The platform pairs an embedded CPU-only computer and a forward-view USB
webcam at $640\times480$, here a Logitech HD~C270 webcam and an embedded board of the NVIDIA Jetson Nano /
Raspberry Pi~4 class. The detector-free pipeline of this paper runs on its CPU with no neural accelerator
(Figure~\ref{fig:camera}). This
robot shares its camera model with a sibling robot whose camera was calibrated from a standard checkerboard
procedure; the intrinsics and lens distortion from that calibration are reused here, as they are a property
of the camera and lens rather than of the mounting. The extrinsics, by contrast, are taken from this robot's
own measured installation: the camera sits on the robot centreline $65$\,cm above the ground with its optical
axis tilted $55^{\circ}$ from the vertical (equivalently $35^{\circ}$ below the horizontal). From these
intrinsics and measured extrinsics an image-to-ground homography is constructed under a flat-ground
assumption (world origin on the centreline beneath the camera) and used to express the near-row cross-track in
centimetres (Section~\ref{sec:method:lookahead}). The residual assumptions are the planar ground and the
reuse of the camera intrinsics, so the cross-track is also reported in pixels and as a fraction of the
half-image width. The centimetre scale is sensitive to the measured pose: perturbing the height by
$\pm5$\,cm and the tilt by $\pm5^{\circ}$ moves the near-row pixel-to-centimetre factor enough to shift the
reported median cross-track over roughly $[0.9,1.3]$\,cm, so the centimetre figures are indicative and the
pixel error is treated as the primary cross-track metric. The scope is fixed to one robot,
one crop (cabbage), and one camera pose. The validation reported here is narrow by design and at a single low
frame rate, with no claim of generalisation beyond this configuration. Integrating the guidance into the
steering loop and measuring the resulting closed-loop tracking is left to future work.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.8\linewidth]{IMG_20251201_162642.jpg}
  \caption{The cabbage-field robot during a field-test session: a four-wheel straddle platform carrying a
  forward-view camera, driving along the cabbage rows. The guidance pipeline runs on the platform's onboard
  CPU; closing the steering loop from the pipeline's output is left to future work.}
  \label{fig:robot}
\end{figure}


\begin{figure}[t]
  \centering
  \includegraphics[width=0.74\linewidth]{IMG_5035.jpg}
  \caption{Onboard camera and compute. The forward-view camera and the embedded computer are carried on
  the four-wheel straddle frame that spans the crop
  bed. The bed shown here is mixed, with cabbage interplanted among other leafy greens, as also
  reflected in the field runs (Section~\ref{sec:res-dataset}).}
  \label{fig:camera}
\end{figure}

\noindent The full sensing-to-actuation hardware path appears in Figure~\ref{fig:hardware}. The
forward-view camera streams over USB to the onboard computer, which runs the detector-free guidance
pipeline and emits a heading and a cross-track offset. A microcontroller and stepper drives turn the
wheels. During the recorded runs the loop was closed by the platform's existing detector-based controller;
for the proposed pipeline only the perception path was run and logged, from camera to guidance output, and
closing the loop from that output to the drive is marked as future work.

\begin{figure}[t]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[font=\footnotesize, >={Latex[length=2mm]},
  blk/.style={draw,rounded corners,align=center,text width=21mm,minimum height=11mm,inner sep=2pt,fill=blue!5},
  stg/.style={draw,rounded corners,align=center,text width=21mm,minimum height=11mm,inner sep=2pt,fill=green!8},
  fut/.style={draw,dashed,rounded corners,align=center,text width=20mm,minimum height=11mm,inner sep=2pt,fill=black!4},
  node distance=8mm and 7mm]
  \node[blk] (cam) {Forward-view camera (C270, $640{\times}480$)};
  \node[stg, right=of cam] (exg) {Excess-Green row tracker};
  \node[stg, right=of exg] (fit) {Huber IRLS fit $x{=}P(y)$};
  \node[stg, right=of fit] (tmp) {Temporal stage (median, EMA, gate)};
  \node[blk, right=of tmp] (out) {Guidance output $\theta,\ e$};
  \draw[->] (cam)--(exg) node[midway,above,font=\scriptsize]{USB};
  \draw[->] (exg)--(fit); \draw[->] (fit)--(tmp); \draw[->] (tmp)--(out);
  \begin{scope}[on background layer]
    \node[draw,rounded corners,fill=green!4,fit=(exg)(tmp),inner sep=5mm] (cbox) {};
  \end{scope}
  \node[anchor=south,font=\scriptsize] at (cbox.north) {Onboard computer (Jetson Nano or Raspberry Pi~4)};
  \node[fut, below=24mm of cam] (ctrl) {Steering controller};
  \node[fut, right=of ctrl] (ard) {Microcontroller};
  \node[fut, right=of ard] (drv) {Motor drivers};
  \node[fut, right=of drv] (mot) {Drive motors and wheels};
  \draw[->,dashed] (ctrl)--(ard); \draw[->,dashed] (ard)--(drv); \draw[->,dashed] (drv)--(mot);
  \draw[->,dashed] (out.south) -- ++(0,-9mm) -| (ctrl.north);
  \begin{scope}[on background layer]
    \node[draw,dashed,rounded corners,fill=black!2,fit=(ctrl)(mot),inner sep=4mm] (fbox) {};
  \end{scope}
\end{tikzpicture}}
\caption{Hardware and data path of the cabbage robot. The solid blocks, sensing and the onboard
detector-free guidance pipeline, were run and logged in this study; the dashed group (steering
controller, microcontroller, stepper drivers, and motors) was not exercised here and shows the intended
closed loop.}
\label{fig:hardware}
\end{figure}

\noindent Two real robot runs recorded on cabbage plots form the frame set: run~1 (approximately 20 minutes)
and run~2 (approximately 14 minutes). Frames are sampled at 1\,frame per second (1\,fps) and screened to the frames showing the cabbage navigation row through the look-ahead region, removing stretches dominated by adjacent leafy-green beds or by occluding operators. This yields 1482 forward-view cabbage frames
in total ($812$ from run~1 and $670$ from run~2), all at $640\times480$. The full set is treated as a pure
test set. Because the pipeline is training-free there is no training/validation split and no possibility of
memorisation. To probe robustness to crop growth stage and canopy density the frames are stratified into
vegetation tertiles (low, mid, and high Excess-Green coverage), giving three roughly equal strata of $491$,
$487$, and $504$ frames. The dataset will be released publicly as the first real forward-view cabbage robot
crop-row corpus, and a Data and Code Availability statement accompanies the manuscript.

% =====================================================================
\subsection{Central-row tracking via the Excess-Green index}
\label{sec:method:exg}
% =====================================================================

\noindent Each frame is reduced to a single-channel vegetation map using the Excess-Green index of Woebbecke
\textit{et~al.}~\cite{woebbecke1995}. From the normalised RGB channels $(r,g,b)$ the index is
\begin{equation}
\label{eq:exg}
\mathrm{ExG} = 2g - r - b ,
\end{equation}
which emphasises live green canopy against soil and residue and is a standard, illumination-tolerant
choice for in-field crop/soil separation~\cite{meyer2008}. The map is thresholded into a binary
vegetation mask by Otsu's method~\cite{otsu1979}, which selects the foreground/background split
automatically for each frame and so adapts to changing lighting and canopy density without a hand-tuned
constant.

Crop rows viewed head-on appear as near-vertical green bands, and the central navigation row is the band the
robot straddles. Tracking it without a detector relies on a per-row column profile. For each image row $y$ the
pipeline sums (or smooth-and-sums) the masked ExG response across columns and takes the column of peak
vegetation density as the candidate row centre $x^{\ast}(y)$ at that height. Scanning $y$ from the image
bottom upward yields a set of central-row anchor points $\{(x^{\ast}(y),y)\}$ that follow the near-vertical
green band. To bias the tracker toward the row the robot is actually in rather than an adjacent one, the
column search is restricted to a \emph{virtual safety corridor}: a lateral band about the robot centreline on
the ground, projected into the image per row through the calibration of Section~\ref{sec:method:platform}.
The platform's straddle chassis spans a single bed of half-width $220$\,mm, so the neighbour rows sit near or
just inside that width. Because a vegetation index cannot tell the central crop from a neighbouring crop, the
seed band is set slightly tighter, at half-width $\pm180$\,mm: when the central row is momentarily gapped, a
$\pm220$\,mm band would still admit the neighbour at the chassis edge and the (memoryless) front end would
re-seed onto it every frame of the gap, whereas the $\pm180$\,mm band excludes it while still admitting the
central row's lateral wander. The corridor is applied when seeding the track at the near row, where the rows
are most separated in the image, so that the track starts on the central row and follows it upward. The output of this
stage is the ordered set of central-row anchors passed to the line fit. Emitting an anchor set is a
self-consistency property and not a correctness guarantee: a peak is found in essentially every frame, but
whether the chosen column corresponds to the true central row is precisely what the ground-truth evaluation of
Section~\ref{sec:setup-metrics} is designed to measure.

% =====================================================================
\subsection{Robust quadratic guidance-curve fitting}
\label{sec:method:fit}
% =====================================================================

\noindent The guidance line is parameterised as image column $x$ as a function of image row $y$,
\begin{equation}
\label{eq:curve}
x = P(y) = c_2\,y^2 + c_1\,y + c_0 ,
\end{equation}
a quadratic in $y$. This orientation is chosen for a reason. Crop rows viewed head-on are near-vertical,
so a conventional $y=mx+c$ fit becomes ill-conditioned as the slope diverges; expressing $x$ as a function
of $y$ removes this vertical-line degeneracy~\cite{sogaard}. The quadratic term $c_2$ captures the gentle
far-field bowing of a row under perspective and mild row curvature, and reduces to the
straight-line case $x = c_1\,y + c_0$ in the limit $c_2\!\to\!0$. The instantaneous local heading at any height
follows from the slope $P'(y)=2c_2 y + c_1$,
\begin{equation}
\label{eq:heading}
\theta(y) = \arctan\!\bigl(P'(y)\bigr),
\end{equation}
reported in degrees from the vertical as the controller-independent navigation quantity; its evaluation at
a look-ahead height is detailed in Section~\ref{sec:method:lookahead}.

Given the $n$ central-row anchors $\{(x_i,y_i)\}$ from Section~\ref{sec:method:exg}, the coefficients
$\mathbf{c}=(c_2,c_1,c_0)$ are obtained by a standard robust quadratic fit $x=P(y)$ by Huber
iteratively-reweighted-least-squares (IRLS) regression of $x$ on $y$~\cite{huber1964}, minimising
\begin{equation}
\label{eq:irls_obj}
\hat{\mathbf{c}} = \arg\min_{\mathbf{c}}\; \sum_{i=1}^{n} \rho_\delta\!\bigl(x_i - P(y_i)\bigr),
\qquad
\rho_\delta(r) =
\begin{cases}
\tfrac{1}{2}r^2, & |r|\le\delta,\\[2pt]
\delta\bigl(|r|-\tfrac{1}{2}\delta\bigr), & |r|>\delta.
\end{cases}
\end{equation}
The coefficients are estimated by standard Huber IRLS with $\delta = 1.345$ and a MAD scale
$\hat\sigma = 1.4826\,\operatorname{median}_i|r_i|$, with a small ridge for conditioning, giving the Huber
influence weights
\begin{equation}
\label{eq:huber_w}
w_i = \min\!\left(1,\ \frac{\delta\,\hat\sigma}{|r_i|}\right),
\qquad \delta = 1.345.
\end{equation}
The constant $\delta=1.345$ is the conventional Huber tuning that retains
$\approx95\%$ efficiency under Gaussian noise while bounding the influence of outliers, which here are
spurious anchors from gaps, weeds, or an adjacent row.

Two standard alternatives serve as comparison fits over the same anchor set, evaluated against the
pipeline's Huber IRLS fit: equal-weight least squares (the non-robust polynomial fit) and RANSAC consensus
fitting~\cite{fischler1981}, a strong robust baseline. The accuracy of the Huber fit relative to these
baselines against human ground truth is reported in Section~\ref{sec:res-accuracy}: the Huber and
equal-weight least-squares fits are statistically indistinguishable and both ahead of RANSAC, so the fit
choice is secondary on the corridor-restricted anchors.

% =====================================================================
\subsection{Heading and cross-track geometry}
\label{sec:method:lookahead}
% =====================================================================

\noindent A controller steers not to the row directly beneath the robot but to where the row is heading some
distance ahead. The guidance geometry is therefore evaluated at a fixed look-ahead image row
$y_{\mathrm{la}}$ in the upper portion of the frame, mirroring the look-ahead point of pure-pursuit and
Stanley-type path trackers~\cite{coulter1992,stanley2006,rajamani2012}. Because the curve $P(y)$ bows in the
far field, the relevant heading is the tangent to the curve at the look-ahead height, not a chord to the image
bottom. The reported heading is therefore the look-ahead tangent
\begin{equation}
\label{eq:heading_la}
\theta = \arctan\!\bigl(P'(y_{\mathrm{la}})\bigr)
       = \arctan\!\bigl(2c_2\,y_{\mathrm{la}} + c_1\bigr),
\end{equation}
the slope of Equation~\eqref{eq:heading} evaluated at $y_{\mathrm{la}}$, in degrees from the vertical.
Using the tangent rather than a global slope aligns the reported heading with the far-field row direction
the robot must follow, and avoids the bias a straight-line fit would incur on a bowed row.

The lateral cross-track offset $e$ is the signed horizontal distance between the robot's reference column,
taken as the image vertical centreline $x_c = W/2$ for a forward-view camera with $W=640$, and the guidance
line evaluated at the look-ahead row,
\begin{equation}
\label{eq:crosstrack}
e = P(y_{\mathrm{la}}) - x_c ,
\end{equation}
in pixels. The offset $e$ is additionally reported normalised by the half-image width $W/2$ (that is, as a
fraction of the half-frame), so that it is dimensionless and comparable across mountings. Conversion to
centimetres requires a metric ground anchor, namely an image-to-ground homography under a flat-ground
assumption. This anchor is the homography of Section~\ref{sec:method:platform}, built from the robot's
measured camera height and tilt; the centimetre cross-track magnitude follows directly, while $e$ is in
all cases also reported in pixels and in \% of half-image width.

% =====================================================================
\subsection{Temporal filtering}
\label{sec:method:fusion}
% =====================================================================

\noindent The per-frame heading $\theta$ and offset $e$ are smoothed over time by three stock,
lightweight operators applied in sequence. Together they form the temporal stage, which is the primary
source of robustness. The chain is far simpler than a full state estimator such as a Kalman
filter~\cite{kalman1960}; its complexity is matched to the 1\,fps operating regime.

The first operator passes the raw heading stream through a short sliding-window median filter. The median is
robust to isolated single-frame outliers, such as a momentary wrong-row peak or a gap-induced bad fit,
without the lag or overshoot of a mean filter. The median-filtered heading is then low-pass filtered by an
exponential moving average,
\begin{equation}
\label{eq:ema}
\hat\theta_t = \beta\,\hat\theta_{t-1} + (1-\beta)\,\tilde\theta_t ,
\end{equation}
where $\tilde\theta_t$ is the median-filtered heading at frame $t$ and $\beta\in[0,1)$ sets the smoothing
horizon. The exponential moving average suppresses high-frequency jitter while remaining causal and $O(1)$ in
memory. The offset $e$ is fused with the identical median$\rightarrow$EMA chain.

A frame-to-frame jump gate completes the chain, rejecting any update whose heading change exceeds a physical
bound $\Delta\theta_{\max}$. When $|\tilde\theta_t - \hat\theta_{t-1}| > \Delta\theta_{\max}$ the update is
gated and the last fused heading is held rather than applied. The bound $\Delta\theta_{\max}$ is set from the
robot's physical heading-rate limit, independently of any evaluation threshold. A ground robot driving a row
cannot reorient faster than some maximum yaw rate $\dot\psi_{\max}$ (deg\,s$^{-1}$), so over one inter-frame
interval $\Delta t$ the largest kinematically plausible heading change is
\begin{equation}
\label{eq:jumpgate}
\Delta\theta_{\max} = \dot\psi_{\max}\,\Delta t ,
\qquad \Delta t = \tfrac{1}{\text{1\,fps}} = 1\,\text{s}.
\end{equation}
Any apparent inter-frame heading change larger than $\Delta\theta_{\max}$ cannot be the vehicle physically
turning; it has to be a perception artefact, such as a jump onto an adjacent row, and is gated. The value
$\dot\psi_{\max}$ is fixed from the platform's turning specification, and hence $\Delta\theta_{\max}$, before
any error metric is examined, so the gate threshold is not tuned to the evaluation. The same physical
reasoning bounds the per-frame change in $e$.

A reduction in large per-frame jumps is not the measure of the gate's effect, since the gate rejects large
jumps by definition and any ``before/after'' jump count is therefore arithmetic rather than evidence of
correctness. Raw-versus-fused jump counts are reported descriptively in Section~\ref{sec:results}, labelled
as such, and are excluded from the robustness results. The substantive robustness claim, that fusion lowers
heading error against human ground truth on the frames the gate suppresses while the gate fires selectively
on poor frames, is evaluated against the labelled subset in Section~\ref{sec:res-accuracy}.

\noindent Figure~\ref{fig:method} shows the complete pipeline applied to a single forward-view cabbage frame, from the Excess-Green track through the robust curve to the look-ahead tangent and the reported heading and cross-track.

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{fig_cabbage_method.png}
  \caption{Detector-free guidance on a real forward-view cabbage frame. \emph{Left}: input frame
  ($640\times480$). \emph{Right}: the Excess-Green column-peak track (orange points), the robust
  Huber IRLS quadratic guidance curve $x=P(y)$ (green), the look-ahead tangent that defines the steering
  heading $\theta$ (red), and the near-row cross-track offset $e$ from the image centre (blue); the
  reported $\theta$ and $e$ are overlaid. No detector and no training are involved.}
  \label{fig:method}
\end{figure}

% =====================================================================
\subsection{Ground-truth annotation protocol}
\label{sec:setup-gtline}
% =====================================================================

\noindent The ground-truth (GT) guidance line used for evaluation is human-annotated and is therefore
independent of the perception pipeline under test. For each labelled cabbage frame an annotator marks the
central navigation row directly, following \emph{convention~A}: the line is labelled along the row the
robot is driving, traced through the cabbage canopy from the image bottom-centre up the inter-row
corridor. The marked points are fit in the same $x=P(y)$ parameterisation of Equation~\eqref{eq:curve}, and
the GT heading is the look-ahead tangent of Equation~\eqref{eq:heading_la} evaluated at the same
$y_{\mathrm{la}}$ as the system output, so that predicted and GT headings are directly comparable. A subset of
$150$ frames was labelled by a single annotator, stratified across the two runs and the vegetation tertiles.
Quantifying the reliability of the protocol would require a second annotator: an inter-annotator agreement,
the median absolute heading difference in degrees and cross-track in pixels, would set a floor on the
smallest meaningful error, and that study is left to future work. Because the GT line is marked by hand on
the image rather than derived from the tracker's anchors, it is independent of the Excess-Green front end and
provides a fair external reference.


% =====================================================================
\subsection{Evaluation metrics and statistics}
\label{sec:setup-metrics}
% =====================================================================

\noindent The primary metric is heading error in degrees, the absolute difference between the system's
look-ahead tangent heading and the human-GT heading, $|\theta - \theta^{\mathrm{gt}}|$, which is
controller-independent. The secondary metric is cross-track error in pixels (and in \% of half-image width),
the lateral offset between the predicted and GT guidance lines at the look-ahead row $y_{\mathrm{la}}$
(Equation~\eqref{eq:crosstrack}). Centimetre units are reported through the image-to-ground
homography of Section~\ref{sec:method:platform}, built from the robot's measured camera pose; the heading and
cross-track errors against the labelled subset are reported in degrees, pixels, and centimetres in
Section~\ref{sec:res-accuracy}.

As a basic sanity check the fraction of frames for which the tracker emits a guidance line at all is reported
as a self-consistency measure, both overall and broken down by vegetation tertile, in
Section~\ref{sec:res-reliability}. Here ``row found'' means a line was emitted, not that the emitted line is
the correct central row; whether the emitted line is correct is measured by the heading and cross-track error
against human GT in Section~\ref{sec:res-accuracy}.

Consistent with Section~\ref{sec:method:fusion}, the robustness of the temporal stage is reported as (i)
heading error versus human GT on the gate-suppressed frames, raw versus fused, and (ii) the selectivity of
the gate, that is, whether the frames it holds carry larger raw errors than the frames it passes; both are
reported in Section~\ref{sec:res-accuracy}. The raw-versus-fused jump count is excluded from the robustness
results. The Huber-fit-versus-baseline comparison (equal-LS, RANSAC) is reported in
Section~\ref{sec:res-accuracy}; the trained-detector baseline is reported there too.

Per-frame errors are written to one CSV row per frame so that the detector-free pipeline and its baselines
and ablations are compared paired on identical frames. Because per-frame errors are not normally distributed,
comparisons are reported as error distributions (median, mean, and 90th percentile) and as paired per-frame
differences rather than as $p$-values. For the load-bearing comparisons the median paired difference is
accompanied by a $95\%$ confidence interval obtained by bootstrap ($10{,}000$ resamples of the paired
per-frame differences); the labelled frames are a sparse stratified sample drawn across both runs, so they
are resampled at the frame level. Where that interval covers zero the difference is reported as
statistically indistinguishable rather than as a ranking. This is the case for the Huber fit against
equal-weight least squares (median paired difference $0^\circ$) and, notably, for the proposed pipeline
against the trained YOLOv5 detector baseline (median $0.2^\circ$, interval $[-0.5,1.2]^\circ$; still
covering zero when the detector is also given the temporal stage, $0.5^\circ$, $[-0.4,1.2]^\circ$).